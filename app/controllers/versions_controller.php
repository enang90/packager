<?php
class VersionsController extends AppController {
	var $view = 'Theme';
	var $theme = 'private';
  var $name = 'Versions';
  var $components = array('Session');
  var $uses = array('User', 'Brand');

  function add() {
	
  }

}